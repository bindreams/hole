{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "minimumReleaseAge": "7 days",
  "postUpdateOptions": ["gomodTidy", "npmDedupe"],
  "platformAutomerge": true,
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/msi-installer/src/msi_installer/wix-toolchain\\.toml$/"
      ],
      "matchStrings": [
        "version\\s*=\\s*\"(?<currentValue>[^\"]+)\"\\n.*github\\.com/(?<depName>[^/]+/[^/]+)/"
      ],
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["/^\\.github/workflows/.+\\.ya?ml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)(?: versioning=(?<versioning>\\S+))?\\s+\\w+:\\s*[\"'](?<currentValue>[^\"']+)[\"']"
      ],
      "extractVersionTemplate": "^v?(?<version>.+)$"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["/^prek\\.toml$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+additional_dependencies\\s*=\\s*\\[\"[^\"=]+==(?<currentValue>[^\"]+)\"\\]"
      ]
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["/^crates/ex-ray/third_party/[^/]+/\\.gitrepo$/"],
      "matchStrings": [
        "remote\\s*=\\s*https://github\\.com/(?<packageName>[^/\\s]+/[^/\\s]+)\\n\\tbranch\\s*=\\s*(?<currentValue>\\S+)"
      ],
      "datasourceTemplate": "github-tags"
    }
  ],
  "packageRules": [
    {
      "description": "Don't update vendored upstream CI under external/*/.circleci/. We don't run it; subrepo pulls overwrite any edits.",
      "matchFileNames": ["external/*/.circleci/**"],
      "enabled": false
    },
    {
      "description": "Group non-major GitHub Actions updates",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "GitHub Actions",
      "automerge": true
    },
    {
      "description": "Group non-major Rust crate updates",
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "Rust crates",
      "automerge": true
    },
    {
      "description": "Group non-major Go module updates",
      "matchManagers": ["gomod"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "Go modules",
      "automerge": true
    },
    {
      "description": "Group non-major Python (PEP 621) updates",
      "matchManagers": ["pep621"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "Python packages",
      "automerge": true
    },
    {
      "description": "Group non-major npm updates; weekly lockfile maintenance runs out-of-band",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "npm packages",
      "automerge": true,
      "lockFileMaintenance": {
        "enabled": true,
        "automerge": true,
        "schedule": ["before 9am on monday"]
      }
    },
    {
      "description": "Major updates: one PR per package (API breaks reviewed individually). groupName: null overrides any inherited monorepo group from config:recommended's group:monorepos / group:recommended presets. Applies to customManagers (WiX) as well — by design; the customManagers block has no groupName, so this just reasserts the default.",
      "matchUpdateTypes": ["major"],
      "groupName": null,
      "automerge": true
    },
    {
      "description": "gsudo (installer-E2E de-elevation): auto-merge; the required Test installer check gates it.",
      "matchDepNames": ["gerardog/gsudo"],
      "automerge": true
    },
    {
      "description": "Tauri ships each plugin as an npm/crate pair and `npx tauri build` refuses a major.minor mismatch (#679). The npm and cargo managers are grouped separately above, so without this a bump on one side lands alone and breaks the DMG bundle. Last rule wins, so this overrides those groups for these packages.",
      "matchPackageNames": ["@tauri-apps/**", "tauri", "tauri-build", "tauri-plugin-**"],
      "groupName": "Tauri"
    },
    {
      "description": "Vendored git-subrepo deps (v2ray-core, utls): auto-merge armed for every update type — a routine bump is typically minor/patch, not major, so the file's major-only rule above doesn't reach these. Safety is entirely the required checks' job (Test ex-ray (Go), and check-vendoring-integrity riding inside Lint), not this rule.",
      "matchFileNames": ["crates/ex-ray/third_party/*/.gitrepo"],
      "automerge": true
    },
    {
      "description": "Don't update the vendored trees' own go.mod/go.sum under crates/ex-ray/third_party/*/. They're pristine upstream code (see VENDORING.md), excluded from this repo's tooling, and pull-subrepo's own allowlist silently overwrites them with upstream's version on the next bump anyway.",
      "matchManagers": ["gomod"],
      "matchFileNames": ["crates/ex-ray/third_party/*/go.mod", "crates/ex-ray/third_party/*/go.sum"],
      "enabled": false
    },
    {
      "description": "github.com/v2fly/v2ray-core/v5 and github.com/refraction-networking/utls in crates/ex-ray/go.mod are locally `replace`d to the vendored third_party trees (see VENDORING.md) — .gitrepo's branch, not this require line, is authoritative for their version. check-vendoring-integrity treats a mismatch as a violation; only finish-vendor-bump may bump this line, in lockstep with .gitrepo.",
      "matchManagers": ["gomod"],
      "matchDepNames": ["github.com/v2fly/v2ray-core/v5", "github.com/refraction-networking/utls"],
      "enabled": false
    },
    {
      "description": "Rust compiler: its own reviewable PR, never automerged. A compiler bump lands new default-on clippy lints across the whole workspace at once (#855), so it must not ride in a batch. `groupName: null` un-groups it; `automerge: false` overrides the major-updates rule above, which matches every manager including this one. Last rule wins — these compiler rules must stay LAST in this array.",
      "matchManagers": ["rust-toolchain"],
      "matchDepNames": ["rust"],
      "groupName": null,
      "automerge": false
    },
    {
      "description": "Go compiler: covers both `crates/ex-ray/go.mod` directives (`toolchain`, what actions/setup-go installs, and `go`, the language level) — both extract as depName `go` under gomod. Never automerged, same reason as Rust. `go` keeps the default `replace` rangeStrategy deliberately: `bump` would raise it to match `toolchain` on every release, and the Go tool then elides a `toolchain` line equal to `go` (deleting the directive and its pin comment) on the next `go mod tidy` — verified locally. `replace` is a no-op while `go`'s range already covers the latest release, which just means routine `go` releases don't produce their own update; a Go major would still surface here, ungrouped-but-caught by this same rule. Last rule wins — this and the rule above must stay LAST in this array.",
      "matchManagers": ["gomod"],
      "matchDepNames": ["go"],
      "groupName": "Go toolchain",
      "automerge": false
    }
  ]
}
