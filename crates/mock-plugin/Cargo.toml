[package]
name = "mock-plugin"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
publish = false

[lints]
workspace = true

[dependencies]
garter = { path = "../garter" }
serde_json = { workspace = true }
tokio = { version = "1", features = ["full"] }
